import ElementEnum from "./enums/elementEnum.js"

const formatRules = Object.freeze({
    NOTTOMERGE: [
        ElementEnum.LI.value,
        ElementEnum.P.value
    ],
    ROOT: [
        
    ]
})

export default formatRules